#include "move.h"

/**
 * \brief Print a message describing a move
 *
 * \param m The move
 * \param name The player name
 * \param round The current number of round
 */
void print_move(struct move_t* m, const char* name, unsigned int round);